point(x,y);
line(x1,y1,x2,y2);

random(high);
random(low,high); // high not included

void setup() {
    size(512, 512);
    background(#ff8000, 60);
}

void draw() {
  // draw stuff
}

println("Hello ErrorLog!");
